name: extendo-render install
description: Installation of the extendo-render app 
for: repositories
where:
  - resource.isPrivate == false

installation:
  uses: apps://extendo-render@v1.0
  with: 
    interestingInput: great value
