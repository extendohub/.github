name: Math equation rendering
description: Render math equations found in markdown

render:
  markdown:
    uses: ./index.js
    with:
      tex:
        inlineMath: [['$', '$'], ['\\(', '\\)']]
      svg: 
        fontCache: global
